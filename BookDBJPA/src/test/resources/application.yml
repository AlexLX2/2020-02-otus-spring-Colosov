spring:
  datasource:
    initialization-mode: always
    schema: test_schema.sql
    data: test_data.sql
  shell:
    interactive:
      enabled: false

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true